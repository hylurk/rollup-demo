
(function(l, i, v, e) { v = l.createElement(i); v.async = 1; v.src = '//' + (location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1'; e = l.getElementsByTagName(i)[0]; e.parentNode.insertBefore(v, e)})(document, 'script');
var Demo = (function () {
  'use strict';

  function styleInject(css, ref) {
    if ( ref === void 0 ) ref = {};
    var insertAt = ref.insertAt;

    if (!css || typeof document === 'undefined') { return; }

    var head = document.head || document.getElementsByTagName('head')[0];
    var style = document.createElement('style');
    style.type = 'text/css';

    if (insertAt === 'top') {
      if (head.firstChild) {
        head.insertBefore(style, head.firstChild);
      } else {
        head.appendChild(style);
      }
    } else {
      head.appendChild(style);
    }

    if (style.styleSheet) {
      style.styleSheet.cssText = css;
    } else {
      style.appendChild(document.createTextNode(css));
    }
  }

  var css = ".rollup-demo{width:1200px;margin:0 auto;height:500px;line-height:500px;background-color:rgba(0,0,0,.1);text-align:center;color:#000;font-size:28px}";
  styleInject(css);

  var $ = function $(ele) {
    return document.querySelector(ele);
  };

  var init = function init(opts) {
    var wrapper = opts.wrapper || 'body';
    var text = opts.text || '这家伙真懒，啥也不传';
    var $el = document.createElement('div');
    $el.classList.add('rollup-demo');
    $el.innerHTML = text;
    $(wrapper).appendChild($el);
  };

  var index = {
    init: init
  };

  return index;

}());
//# sourceMappingURL=demo.min.js.map
